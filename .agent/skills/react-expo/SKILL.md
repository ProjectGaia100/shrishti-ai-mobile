---
name: react-native-expo
description: Best practices and conventions for React Native and Expo development. Use this when writing UI components, routing, or debugging Expo apps.
---

# Expo Guidelines

- Always use ` npx expo start --localhost --clear` for running the local server then send 'w' to open in browser.
- Default to using Expo Router for navigation instead of React Navigation.
